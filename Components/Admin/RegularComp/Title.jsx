import React from "react";

const Title = ({ title }) => {
  return (
    <div className="col-12 mt-3">
      <h5 style={{ color: "#fff", marginBottom: "20px" }}>{title}</h5>
    </div>
  );
};

export default Title;
